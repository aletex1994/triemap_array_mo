service : {
  clearWhitelist: () -> (vec record {
                               text;
                               nat;
                             });
  getSize: () -> (nat);
  lookup: (text) -> (opt nat);
  register: (text) -> (nat);
  showWhitelist: () -> (vec record {
                              text;
                              nat;
                            });
}
